// RemzDNB - 2026

using BepInEx.Configuration;

namespace RZEssentialsClient;

internal static class Settings
{
    // Raid Menus
    /*public static ConfigEntry<bool> SkipSideSelectionScreen { get; private set; }
    public static ConfigEntry<bool> SkipInsuranceScreen { get; private set; }
    public static ConfigEntry<bool> SkipRaidSettingsScreen { get; private set; }
    public static ConfigEntry<bool> SkipExperienceScreen { get; private set; }*/
    
    public static void Init(ConfigFile config)
    {
        /*SkipSideSelectionScreen = config.Bind("Raid Menus", "Skip Side Selection (pre raid)", true, "");
        SkipInsuranceScreen = config.Bind("Raid Menus", "Skip Insurance Screen (pre raid)", true, "");
        SkipRaidSettingsScreen = config.Bind("Raid Menus", "Skip Raid Settings Screen (pre raid)", true, "");
        SkipExperienceScreen = config.Bind("Raid Menus", "Skip Experience Screen (post raid)", true, "");*/
    }
}